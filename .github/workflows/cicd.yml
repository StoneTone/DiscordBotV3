name: Discord CICD

on:
  push:
    branches:
      - master
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v5

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Build youtube-source fork
        run: |
          git clone https://github.com/ashton045/youtube-source.git /tmp/youtube-source
          cd /tmp/youtube-source
          git checkout cf77f5b59d6fc27c3be0809cc3e90aad6f8efc30
          chmod +x gradlew
          ./gradlew publishToMavenLocal -x test --no-daemon

      - name: Build with Gradle
        run: |
          chmod +x gradlew
          ./gradlew clean build -x test --no-daemon
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          GUILD_ID: ${{ secrets.GUILD_ID }}
          GPT_SECRET: ${{ secrets.GPT_SECRET }}

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v5
        with:
          name: jar-file
          path: build/libs/*.jar

  containerize:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Code
        uses: actions/checkout@v5

      - name: Download Build Artifact
        uses: actions/download-artifact@v5
        with:
          name: jar-file
          path: build/libs/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          platforms: linux/amd64,linux/arm64
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/discordbotv3:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
  deploy:
    needs: containerize
    runs-on: ubuntu-latest
    steps:
      - name: Setup Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Deploy to ApplePi
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.TAILSCALE_APPLEPI_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}
            docker compose down
            docker compose pull
            docker compose up -d
